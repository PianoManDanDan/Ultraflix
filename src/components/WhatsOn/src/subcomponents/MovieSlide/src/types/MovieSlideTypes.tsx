import { ImageProps } from '../../../../../../Image/src/types';

export type MovieSlideProps = {
	posterImage: ImageProps;
	title: string;
	runtime: string;
	certificate: string;
	releaseYear: number;
};
